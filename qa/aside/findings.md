# 검토된 발견사항과 수정 이력

조회 전용 Aside 실행으로 발견한 후보를 코드 및 실제 화면과 대조해 검토했다.
원본 실행 결과는 `artifacts/aside/<runId>/`에 보존하고 비밀/개인정보를 제거한 검토 결과만 여기에 옮긴다.

| ID  | 종류 | 요구사항/케이스 | 발견 실행·증거 | 재현·원인 | 상태 | 수정 커밋 | 재검증 실행·결과 |
| --- | ---- | --------------- | -------------- | --------- | ---- | --------- | ---------------- |

상태: REPORTED → REPRODUCED → FIXED_PENDING_RETEST → VERIFIED. 재현 안 됨은 NEEDS_EVIDENCE, 환경 제약은 BLOCKED. 개선 제안은 PROPOSED → APPROVED 뒤에만 구현한다.
결함 ID 예: `<runId>-4-b-B`. 같은 증상의 여러 실행은 별도 결함으로 무한 생성하지 말고 기존 결함에 재현 실행을 연결한다.
코드 수정만으로 VERIFIED 처리하지 않는다. 실제 재검증이 BLOCKED이면 FIXED_PENDING_RETEST 상태를 유지한다.

## 2026-09-23 조회 전용 실행 검토

실행 ID: `2026-09-23T01-21-44-856Z-f69b97b0`. 로컬 개발 앱에서 Aside가 28개 말단 케이스를 실행했다. 원본 관찰은 PASS 19 / FAIL 후보 2 / BLOCKED 7이며, 아래 판정은 코드와 재현 근거를 검토한 결과다. 원본 로그와 스크린샷은 Git 제외 경로에만 있다.

| ID | 종류 | 기준/케이스 | 발견·재현·원인 | 상태 | 변경·회귀 검증 | 커밋·재검증 |
| --- | --- | --- | --- | --- | --- | --- |
| `2026-09-23T01-21-44-856Z-f69b97b0-1-c-D` | 오탐 | R01 / 1-c-D | Aside가 접근성 상태 이름 `페이지 로딩`을 화면 문구로 해석했다. `src/app/loading.tsx`에는 화면 텍스트 노드가 없고 `aria-label`과 회전 SVG만 있다. | REJECTED_FALSE_POSITIVE | 기준·케이스에 접근성 이름 허용을 명시. 시각적 로딩·모션 감소는 추가 브라우저/실기기 확인 대상. | `b97ac6e`; 코드·화면 구조로 오탐 판정, 새 기준의 전체 실행은 미수행 |
| `2026-09-23T01-21-44-856Z-f69b97b0-6-a-A` | 오탐 | R06 / 6-a-A | Aside가 보고서 카드의 수업 제목을 작성 메타정보로 해석했다. `src/features/reports/list.tsx`의 제목과 `ReportMetadata`의 이름·일시는 별도 요소이며 접두어·양식 버전·상태 뱃지가 없다. | REJECTED_FALSE_POSITIVE | 기준·케이스에 제목 수업명 허용과 작성 메타 범위를 명시. | `b97ac6e`; 코드·화면 구조로 오탐 판정, 새 기준의 전체 실행은 미수행 |
| `2026-09-23T01-21-44-856Z-f69b97b0-manage-copy` | BUG | R04 / 4-a-E | 관리 화면을 조회하자 `보고서 확인`, `확인해 주세요`가 남아 있었다. 승인/확인 기능을 삭제한 최신 기준과 어긋난 과거 안내 문구이며 `src/app/(work)/manage/page.tsx`에서 재현된다. | VERIFIED | 메뉴를 `전체 보고서`와 조회 안내로 변경하고 조회 전용 회귀 케이스 4-a-E 추가. | `b97ac6e`; Aside MCP로 로컬 `/manage`를 다시 조회해 `전체 보고서 작성된 보고서를 살펴보세요.` 표시 확인 |
| `2026-09-23T01-21-44-856Z-f69b97b0-parser` | QA 도구 결함 | 결과 집계 | CLI가 결과 종료 마커 뒤에 ANSI 색상 코드를 붙였다. 엄격한 마커 정규식이 원본 28개를 모두 NOT_RUN으로 오인했다. | VERIFIED | 색상 제어 코드 제거 후 형식 검증. `aside-report.test.mjs` 회귀 테스트 통과. | `b97ac6e`; `npm run qa:review -- 2026-09-23T01-21-44-856Z-f69b97b0`로 원본 출력을 재검증해 PASS 19 / FAIL 2 / BLOCKED 7 자동 집계 확인 |

## 2026-09-23 조회 재실행 및 전제조건 보완

재실행 ID: `2026-09-23T01-53-53-604Z-91e03555`. 29개 말단 케이스 중 PASS 28 / FAIL 0 / BLOCKED 1. 앞선 실패 후보 `6-a-A`는 PASS, `1-c-D`는 로딩 접근성 상태와 화면 텍스트 부재까지 확인했으나 화면 캡처가 CDP 시간 초과되어 회전 그래픽의 시각 상태는 미검증이다. 따라서 `1-c-D`를 VERIFIED로 표시하지 않는다.

앞선 전제조건 부족 7건 `1-b-B`, `1-c-A`, `1-c-B`, `4-a-D`, `6-a-C`, `6-b-B`, `6-b-C`는 모두 재실행에서 PASS. 원인은 각각 클릭 타임아웃, Aside 뷰포트 API 미지원, 다른 작성자/동일 수업명 회차 보고서 데이터 부족이었다. Enter 키 조작과 임시 동일 출처 iframe 390/1280px 측정 절차를 시나리오에 기록했다. 개발용 Supabase에 비메일 QA 코치 1명과 기존 2026-09-23 수업의 임시저장 보고서 1건을 명시적 스크립트로 생성했다(인증정보는 기록하지 않음). 이번 테스트 데이터는 자동 삭제하지 않는다.

| ID | 종류 | 원인 | 상태 | 변경·회귀 테스트 | 커밋·재검증 |
| --- | --- | --- | --- | --- | --- |
| `2026-09-23T01-53-53-604Z-91e03555-parser` | QA 도구 결함 | Aside가 큐 후속 지시 뒤 동일 판정의 결과 블록을 두 번 출력했고, 미완성 중간 블록도 남겼다. 기존 파서는 전부 NOT_RUN으로 처리했다. | FIXED_PENDING_RETEST | 완전한 한 줄 JSON 블록만 추출하고 복수 블록은 ID별 판정 일치 시 최신 본문을 채택, 불일치 시 거절. 단위 테스트 통과. | 현재 브랜치 커밋 예정; 같은 원본 `npm run qa:review -- 2026-09-23T01-53-53-604Z-91e03555` 재집계 PASS 28 / BLOCKED 1. 다음 CLI 실행에서 재검증 필요 |

로그·스크린샷·다운로드는 `artifacts/aside/`에만 두고 Git에 포함하지 않는다. 이 결과는 로컬 개발 앱과 개발 DB에 관한 것이며 배포/실기기 검증을 뜻하지 않는다.

## 2026-09-23 생성·제출 43건 실행 검토

실행 ID: `2026-09-23T02-07-50-986Z-935a551d`, 브랜치 `test/aside-workflow-43`. Aside가 비로그인 탭을 먼저 열어 43건 BLOCKED 블록을 출력했다. 로그인된 관리자 탭에 다시 연결한 뒤 완전한 43건 결과 블록 2개가 나왔고 두 블록의 판정은 같았다. `npm run qa:review -- 2026-09-23T02-07-50-986Z-935a551d stdout.log 2`로 세 번째 완전 블록(0부터 센 번호 2)을 명시적으로 채택했다. 자동 집계 원본은 PASS 24 / FAIL 후보 5 / BLOCKED 14. 다중 블록을 무조건 수용하지 않도록 기본 파서는 계속 불일치 판정을 거절한다.

이후 같은 Aside 세션에서 허용된 임시 폴더의 비개인 합성 JPEG/PNG로 사진 `5-c-A-1`, `5-c-A-2`, `5-c-B`, `5-c-C`, `5-c-D`를 각각 PASS로 재검증했다. QA 보고서에 사진 3장까지 추가하고 이번 실행에서 추가한 1장만 삭제해 2장을 남겼다. 내려받은 XLSX를 로컬 ExcelJS로 열어 수업명·작성자·한글 답변·인원 3을 확인해 `8-a-C`도 PASS로 보완했다. 이 후속 검토를 반영한 사람 검토 집계는 **PASS 30 / FAIL 후보 5 / BLOCKED 8**이다. 후속 사진 답변에는 별도 구조화 블록이 없으므로 `results.json`의 24/5/14는 원본 블록 수치로 남기고, 30/5/8을 별도 검토 수치로 구분한다.

| ID | 종류 | 실제 관찰과 대조 | 상태 | 변경·회귀 테스트 | 재검증 |
| --- | --- | --- | --- | --- | --- |
| `2026-09-23T02-07-50-986Z-935a551d-3-b-E` | BUG 후보 | Aside UI 조작에서 날짜 단계의 `다음` 뒤 확인 화면 대신 수업 상세로 이동해 수업이 생성됐다. 그러나 `ClassWizard` 코드에는 STEP 4 확인과 별도 등록 버튼이 있다. 미저장 독립 브라우저 조작에서도 같은 현상이 나타나 QA 수업 1건이 추가로 생성됐다. 원인(앱/Aside 조작/개발 서버)은 미확정이다. | NEEDS_EVIDENCE | 4단계 확인·별도 등록 버튼 회귀 테스트 강화, 통과. 제품 수정은 보류 | 별도 브라우저/실기기에서 네트워크 요청과 단계 DOM을 함께 재현해야 함 |
| `2026-09-23T02-07-50-986Z-935a551d-3-c-A-D-F-G` | 연관 BUG 후보 4건 | 일·주·월 예상 날짜 미리보기가 보이지 않았다는 관찰. 해당 미리보기는 STEP 4에만 표시되므로 위 단계 건너뜀 현상과 연관 가능. `occurrenceDates`의 2027/2028 말일 계산 테스트는 모두 통과. | NEEDS_EVIDENCE | 2027-02-28/2028-02-29 회귀 테스트 통과. 실제 제품 변경 없음 | 3-b-E 원인 확인 후 동일 UI 재검증 |
| `2026-09-23T02-07-50-986Z-935a551d-photos` | 도구 전제조건 | Aside 업로드가 프로젝트 폴더 경로를 거부했다. 합성 이미지를 Aside 세션 `tmp`에 생성한 후 사진 5건 PASS. | VERIFIED | `prepare-aside-image-fixtures.mjs --aside-session <ID>` 추가. 기존 사진은 변경하지 않음 | 같은 실행의 후속 사진 재검증; 보고서 조회에서 2장 유지 |
| `2026-09-23T02-07-50-986Z-935a551d-xlsx` | 도구 전제조건 | Aside가 파일 내부를 읽지 못해 `8-a-C`를 BLOCKED로 보고했다. 내려받은 XLSX를 별도 로컬 도구로 열어 한글·작성자·인원 3 검증. | VERIFIED | 제품 변경 없음 | 다운로드 원본 ExcelJS 읽기 성공 |

잔여 BLOCKED: `3-c-B`, `3-c-C`, `3-c-H`, `3-d-A`, `3-d-B`, `3-d-C`, `5-a-D`, `5-b-B`. 추가 생성 위험이 있는 분기는 중단했고, `help_text` 없는 필드 전제조건은 찾지 못했다. 이들은 PASS가 아니다.

쓰기 범위: 접두사 QA 단일 수업 1건, 주간 수업 2건, 그 단일 수업의 보고서 1건, XLSX/PDF 내보내기 각 1건. 독립 확인 중 예상치 못하게 추가된 `QA 미저장 검증` 수업 1건(`aeab1898-9008-4613-88f6-490874f1220b`)도 개발 DB에 남아 있다. 자동 취소·삭제하지 않았다. 테스트 데이터 외 기존 수업·계정·권한·메일은 변경하지 않았다. 인증정보와 개인 데이터는 기록하지 않는다.

## 2026-09-23 수업 등록 결함 수정·직접 브라우저 재검증

브랜치 `fix/qa-wizard-confirmation`에서 기존 43건의 FAIL 후보를 `ClassWizard` 소스와 로컬 로그인 브라우저 `/classes/new`에서 다시 비교했다. 재현 중 폼의 `submit` 이벤트를 테스트 탭에서 선제 차단하여 새 수업을 생성하지 않았다. 수정 전에는 STEP 3의 `type=button` 다음 버튼 클릭 중 React가 동일 DOM 노드를 STEP 4 제출 버튼으로 재사용해 한 번의 클릭에서 `submit` 이벤트가 발생했다. 수정 후 다음/등록 버튼에 다른 `key`와 명시적 `type`을 부여하여 노드를 교체했고, 클릭 후 STEP 4 미리보기만 나타나며 `submit` 이벤트가 발생하지 않음을 관찰했다.

| 발견 ID / 케이스 | 원인·변경 | 상태 | 회귀 테스트 | 브라우저 재검증 |
| --- | --- | --- | --- | --- |
| `2026-09-23T02-07-50-986Z-935a551d-3-b-E` | 다음/등록 버튼의 DOM 재사용으로 조기 제출. 서로 다른 노드로 렌더링 | VERIFIED | `class-wizard.test.tsx`: 버튼 노드 교체·4단계 확인 | STEP 3 다음 1회 → STEP 4, 제출 이벤트 없음 |
| `2026-09-23T02-07-50-986Z-935a551d-3-c-A-D-F-G` | 조기 제출로 STEP 4 미리보기를 관찰하지 못함. 위 수정을 공유 | VERIFIED | `class-wizard.test.tsx`, `recurrence.test.ts` | 일/주 각 2일, 2027-02-28 및 2028-02-29 포함 월말 3일 미리보기 확인, 등록 안 함 |
| `2026-09-23T02-07-50-986Z-935a551d-3-c-B` | 휠 항목 버튼들이 부모 `label`에 감싸져 클릭이 첫 버튼으로 전달될 수 있었음. 일반 컨테이너로 변경 | VERIFIED | `class-wizard.test.tsx`: label 밖 휠·간격 2 | 실제 항목 클릭 `1 → 2` 유지, 9/23·9/25·9/27 미리보기 |
| `2026-09-23T02-07-50-986Z-935a551d-3-d-C` | 익일 종료는 유효하지만 확인 화면에 종료 날짜가 생략됨. 날짜가 다를 때 양쪽 날짜를 표시 | VERIFIED | `class-wizard.test.tsx`: 익일 종료 날짜 문구 | 9/23 23:00 ~ 9/24 01:00 표시, 등록 안 함 |

추가로 직접 브라우저에서 `3-c-C` 요일 미선택, `3-c-H` 종료일 역전, `3-d-A` 09:00~10:00, `3-d-B` 동일/이른 종료시간을 확인했다. 모두 기대 동작으로 PASS이며 새 데이터는 생성하지 않았다. `5-a-D`(빈 수량 저장)와 `5-b-B`(도움말 없는 필드)는 구성요소 회귀 테스트만 통과했으며 실제 저장/화면 전제조건을 재현하지 않아 브라우저 BLOCKED로 남는다. 따라서 앞선 43건의 여러 실행·직접 재검증을 합친 사람 검토치는 **PASS 41 / FAIL 0 / BLOCKED 2**이다. 단일 CLI 실행의 원본 수치로 오인하지 않는다. 수정 커밋과 재검증 결과는 병합 전 Git 기록에 연결한다.
